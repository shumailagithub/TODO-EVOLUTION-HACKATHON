# Implementation Tasks: Todo Chatbot with NLP + NeonDB

## Task Breakdown

### TASK-01: Verify Database Schema & Views
**Description**: Confirm that the `tasks_with_serial` VIEW exists and functions correctly for serial number generation.

**Files/Modules to touch**:
- Database schema inspection
- `tasks_with_serial` VIEW definition
- Database connection utilities

**Acceptance Criteria**:
- DB: VIEW generates sequential serial numbers (1, 2, 3, etc.) for tasks
- DB: Serial numbers renumber automatically after deletions (if tasks 1,2,3 exist and #2 deleted, remaining tasks become 1,2)
- Chatbot: N/A (database verification only)

### TASK-02: Implement CREATE Endpoint Verification
**Description**: Add verification SELECT query after INSERT operations in task creation endpoints.

**Files/Modules to touch**:
- `backend/api/tasks.py` - task creation endpoint
- `backend/services/task_service.py` - task creation service
- Database query utilities

**Acceptance Criteria**:
- DB: After INSERT, verification SELECT confirms record exists with correct UUID and description
- Chatbot: Only reports "task added" after database verification confirms persistence
- DB: If verification fails, transaction is rolled back and error returned

### TASK-03: Implement UPDATE Endpoint Verification
**Description**: Add verification SELECT query after UPDATE operations in task modification endpoints.

**Files/Modules to touch**:
- `backend/api/tasks.py` - task update endpoint
- `backend/services/task_service.py` - task update service
- Database query utilities

**Acceptance Criteria**:
- DB: After UPDATE, verification SELECT confirms correct UUID was modified with expected values
- Chatbot: Only reports "task updated" after database verification confirms change
- DB: If verification fails, appropriate error is returned without claiming success

### TASK-04: Implement DELETE Endpoint Verification
**Description**: Add verification SELECT query after DELETE operations to confirm removal.

**Files/Modules to touch**:
- `backend/api/tasks.py` - task deletion endpoint
- `backend/services/task_service.py` - task deletion service
- Database query utilities

**Acceptance Criteria**:
- DB: After DELETE, verification SELECT confirms record no longer exists (0 results)
- Chatbot: Only reports "task deleted" after database verification confirms removal
- DB: If verification fails, appropriate error is returned without claiming success

### TASK-05: Implement COMPLETE Endpoint Verification
**Description**: Add verification SELECT query after task completion status changes.

**Files/Modules to touch**:
- `backend/api/tasks.py` - task completion endpoint
- `backend/services/task_service.py` - task completion service
- Database query utilities

**Acceptance Criteria**:
- DB: After completion UPDATE, verification SELECT confirms task UUID has completed=true
- Chatbot: Only reports "task completed" after database verification confirms status change
- DB: If verification fails, appropriate error is returned without claiming success

### TASK-06: Ensure tasks_with_serial Used for All Reads
**Description**: Update all read operations to use the `tasks_with_serial` VIEW for consistent serial numbers.

**Files/Modules to touch**:
- `backend/api/tasks.py` - task listing endpoint
- `backend/services/task_service.py` - task retrieval service
- Database query utilities

**Acceptance Criteria**:
- DB: All listing operations query the `tasks_with_serial` VIEW
- Chatbot: All task lists show accurate, current serial numbers that reflect any deletions/additions
- DB: Serial numbers in responses match those generated by the VIEW

### TASK-07: Create Chat Endpoint for NLP Integration
**Description**: Build a new endpoint that processes natural language commands and calls verified API endpoints.

**Files/Modules to touch**:
- `backend/api/chat.py` - new chat endpoint
- `backend/services/chat_service.py` - NLP processing service
- `backend/services/nlp_parser.py` - intent recognition module

**Acceptance Criteria**:
- DB: All operations triggered by chat endpoint follow verification protocols
- Chatbot: Natural language commands (add, complete, delete, list) are correctly interpreted and executed
- DB: Chat endpoint only reports success after database verification confirms persistence

### TASK-08: Implement Serial Number to UUID Mapping
**Description**: Create utilities to map between user-facing serial numbers and internal UUIDs.

**Files/Modules to touch**:
- `backend/services/serial_mapper.py` - serial number to UUID mapping utility
- `backend/services/task_service.py` - integration with existing services

**Acceptance Criteria**:
- DB: Serial number inputs correctly map to UUIDs before database operations
- Chatbot: Users can reference tasks by serial number (e.g., "complete task 3") which resolves to correct UUID
- DB: Mapping respects user isolation (users can only access their own tasks)

### TASK-09: Prevent Chatbot Success Reports on Failed Operations
**Description**: Ensure chatbot responses only report success after database verification confirms persistence.

**Files/Modules to touch**:
- `backend/api/chat.py` - chat endpoint response logic
- `backend/services/chat_service.py` - response handling
- Error handling utilities

**Acceptance Criteria**:
- DB: Failed database operations result in appropriate error responses
- Chatbot: No success messages are sent to users when database operations fail
- DB: Transaction rollback occurs when verification fails

### TASK-10: Normalize Error Responses Across Endpoints
**Description**: Standardize error response format across all API endpoints for consistent user experience.

**Files/Modules to touch**:
- `backend/api/tasks.py` - task endpoint error handling
- `backend/api/chat.py` - chat endpoint error handling
- `backend/api/auth.py` - authentication error handling
- Error response utilities/common modules

**Acceptance Criteria**:
- DB: All error responses follow consistent format with error codes and details
- Chatbot: Error messages are user-friendly and provide actionable information
- DB: Technical details are hidden from users but available for debugging

### TASK-11: Fix Serial Renumbering After Delete Operations
**Description**: Ensure serial numbers are properly renumbered after tasks are deleted.

**Files/Modules to touch**:
- `backend/services/task_service.py` - delete service logic
- `backend/api/tasks.py` - delete endpoint
- Database triggers/VIEW definitions (if needed)

**Acceptance Criteria**:
- DB: After deletion, remaining tasks have consecutive serial numbers starting from 1
- Chatbot: Subsequent task lists show correctly renumbered serial numbers
- DB: VIEW automatically handles renumbering without additional application logic

### TASK-12: NLP Serial Number Validation Hardening
**Description**: Strengthen validation of serial numbers extracted from natural language commands.

**Files/Modules to touch**:
- `backend/services/nlp_parser.py` - intent recognition and validation
- `backend/services/serial_validator.py` - serial number validation utility
- `backend/services/serial_mapper.py` - validation integration

**Acceptance Criteria**:
- DB: Invalid serial numbers (non-existent tasks) are caught before database operations
- Chatbot: Users receive clear error messages when referencing invalid serial numbers
- DB: Validation occurs before any database queries are executed

### TASK-13: Implement Comprehensive Error Logging
**Description**: Add detailed logging for debugging while keeping user-facing messages clean.

**Files/Modules to touch**:
- Logging utilities
- All service modules
- API endpoint handlers

**Acceptance Criteria**:
- DB: Database errors are logged with sufficient detail for debugging
- Chatbot: User-facing messages remain clean and user-friendly
- DB: Sensitive information is not logged inappropriately

### TASK-14: Create Integration Tests for Verification Logic
**Description**: Develop tests that verify the verification SELECT queries work correctly.

**Files/Modules to touch**:
- `backend/tests/test_verification.py` - new verification tests
- `backend/tests/test_chat.py` - chat functionality tests
- Test utilities and fixtures

**Acceptance Criteria**:
- DB: Tests confirm that verification queries detect failed operations
- Chatbot: Tests verify that failed operations don't result in success messages
- DB: Both success and failure scenarios are tested thoroughly

### TASK-15: End-to-End Validation of Complete Flow
**Description**: Test the complete flow from natural language input to verified database operation.

**Files/Modules to touch**:
- Integration test suite
- End-to-end test scenarios
- Test utilities

**Acceptance Criteria**:
- DB: Complete operations (add, complete, delete, list) all follow verification protocols
- Chatbot: Full user workflows work correctly with accurate serial numbers
- DB: All edge cases and error conditions are handled appropriately